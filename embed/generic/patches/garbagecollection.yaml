machine:
  kubelet:
    extraConfig:
      # Start collecting when disk is xx% full
      imageGCHighThresholdPercent: 60
      # Stop collecting when disk drops to xx%
      imageGCLowThresholdPercent: 50
      # Only collect images older than x minutes
      imageMinimumGCAge: "5m"